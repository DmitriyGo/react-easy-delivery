interface IItem {
    id: string;
    name: string;
    description: string;
    price: number;
    amount: number;
}

interface IMeal {
    id: string;
    name: string;
    description: string;
    price: number;
}

interface IUser {
    name: string,
    phone: string,
    email: string
}